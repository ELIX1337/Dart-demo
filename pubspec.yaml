name: periphy
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/ELIX1337/periphy

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  flutter: 
      sdk: flutter

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
