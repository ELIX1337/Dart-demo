name: dart_project
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/ELIX1337/Dart-demo

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
